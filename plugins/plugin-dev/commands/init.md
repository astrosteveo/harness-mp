---
description: Bootstrap plugin-dev skills and agents into the project's .claude directory
argument-hint: <none>
allowed-tools: ["Bash"]
---

# Bootstrap Plugin-Dev Plugin

Run the bootstrap script to copy plugin-dev templates into this project's `.claude/` directory.

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.sh"
```

Report the output to the user.
