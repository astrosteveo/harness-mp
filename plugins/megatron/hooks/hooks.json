{
  "description": "Megatron memory hooks for automatic session indexing",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/bin/bash \"${CLAUDE_PLUGIN_ROOT}/hooks/auto-sync.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}