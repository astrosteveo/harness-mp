---
description: Bootstrap harness skills and agents into the project's .claude directory
argument-hint: <none>
allowed-tools: ["Bash"]
---

# Bootstrap Harness Plugin

Run the bootstrap script to copy harness templates into this project's `.claude/` directory.

```bash
!bash "${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.sh"
```

Report the output to the user.
